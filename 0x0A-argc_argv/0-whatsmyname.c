#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: the number of arguments passed in the program from the command line
 * @argv: the name of array of arguments
 * return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);

}
